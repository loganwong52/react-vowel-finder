// components/UserInput.js
import React from 'react'

function UserInput(props) { // notice that we take in a parameter for our functional component, which is an object that captures ALL props that are passed down
    // render
    return (
        <div>
            {/* <input id="user-input" {props.handleClick}></input> */}
        </div >
    )
}

export default MyButton;